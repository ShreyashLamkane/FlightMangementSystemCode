server:
  port: 8088
  
  
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/flight_security
    username: root
    password: Sherlock06#
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        
  config:
    import: configserver:http://localhost:8084/
    
  application:
    name: FLIGHT-SECURITY