server:
  port: 8089
  
spring:
   config:
    import: configserver:http://localhost:8084/
    
 
   datasource:
    url: jdbc:mysql://localhost:3306/flight_payment
    username: root
    password: Sherlock06#
    driver-class-name: com.mysql.cj.jdbc.Driver
   jpa:
      hibernate:
        ddl-auto: update
      properties:
        hibernate:
         dialect: org.hibernate.dialect.MySQL8Dialect